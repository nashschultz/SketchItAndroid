<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nashschultz.sketchit">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SpecificDrawingActivity" />
        <activity android:name=".GameSpecificActivity" />
        <activity android:name=".GameSummaryActivity" />
        <activity android:name=".GuessActivity" />
        <activity android:name=".DrawActivity" />
        <activity android:name=".NewWordActivity" />
        <activity android:name=".JoinGameActivity" />
        <activity android:name=".CreateGameActivity" />
        <activity android:name=".MainMenu" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5912556187565517~7376705917"/>
    </application>

</manifest>